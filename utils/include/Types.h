#ifndef TYPES_H
#define TYPES_H

#include <string>
#include <vector>

using StringList = std::vector<std::string>;
using StringGrid = std::vector<std::vector<std::string>>;

using IntList = std::vector<int>;
using IntGrid = std::vector<std::vector<int>>;

using LongList = std::vector<long>;
using LongGrid = std::vector<std::vector<long>>;

using DoubleList = std::vector<double>;
using DoubleGrid = std::vector<std::vector<double>>;

#endif // TYPES_H
